# Specification: branding-update

## ADDED Requirements
- The CLI welcome message MUST use the name "codepi" instead of "mypi".

#### Scenario: Welcome Message
- **WHEN** the user starts the CLI
- **THEN** the terminal displays "codepi - AI Coding Agent"

## MODIFIED Requirements
- All instances of the string "mypi" in user-facing UI/CLI messages MUST be updated to "codep" where appropriate for branding.
