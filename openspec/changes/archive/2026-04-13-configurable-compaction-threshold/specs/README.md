## ADDED Requirements

_None — this change only modifies default parameter values in code. No new capabilities are introduced._

## MODIFIED Requirements

_None — the compaction threshold is an implementation default, not a spec-level requirement._
